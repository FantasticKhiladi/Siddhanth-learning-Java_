	Scanner sc = new Scanner(System.in);
		int n =sc.nextInt();
		for(int i=2;i<=n/2;i++)             // to find factors other than 1 and numbers itself
		{ if (n%i ==0)
			System.out.print(i + " ");
		} 
		return;    //if we do not use return then also it works
		
		}
	}
